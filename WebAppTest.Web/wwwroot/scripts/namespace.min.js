var webapptest = webapptest || {};
webapptest.namespace = function (a) {
    var b = a.split("."), c = webapptest, d;
    b[0] === "webapptest" && (b = b.slice(1));
    for (d = 0; d < b.length; d += 1) {
        typeof c[b[d]] == "undefined" && (c[b[d]] = {});
        c = c[b[d]];
    }
    return c;
}