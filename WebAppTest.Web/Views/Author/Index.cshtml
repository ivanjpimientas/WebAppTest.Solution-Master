@model IEnumerable<WebAppTest.Core.Entities.Author>
@{
    ViewData["Title"] = "Authors Manager";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Authors Manager</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Main</a></li>
                    <li class="breadcrumb-item active">Authors</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <!-- Default box -->
                <div class="card">
                    <div class="card-header with-border">
                        <button id="btnAddAuthor" type="button" class="btn btn-primary" onclick="webapptest.authors_manager.displayAddEditAuthor(0)">Add Author</button>

                        <div class="card-tools">
                            <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                                <i class="fas fa-minus"></i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <table id="tblAuthors" class="table table-bordered table-striped dt-responsive">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Name</th>
                                    <th>Country</th>
                                    <th>Email</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Country)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                        <td>
                                            <button class="btn btn-default" onclick="webapptest.authors_manager.displayAddEditAuthor(@item.Id)">Edit</button>
                                            <button class="btn btn-danger" onclick="webapptest.authors_manager.displayDeleteAuthor(@item.Id)"> Delete</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <div class="modal fade" id="authorFormModel" role="dialog">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header bg-info">
                                        <h3 id="title" class="modal-title">Create Author</h3>
                                        <a href="#" class="close" data-dismiss="modal">×</a>
                                    </div>
                                    <div class="modal-body" id="authorFormModelDiv">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- /.box-body -->
                    <div class="card-footer">
                        Footer
                    </div>
                    <!-- /.box-footer-->
                </div>
                <!-- /.box -->
            </div>
        </div>
    </div>
</section>
<!-- /.content -->

@section Scripts {
    <script src="~/scripts/main/webapptest.authors_manager.js"></script>

    <!-- page script -->
    <script>
        $(function () {
            webapptest.authors_manager.displayListAuthors();
        })
    </script>
}